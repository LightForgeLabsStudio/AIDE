# PR Review Workflow Test (Do Not Merge)

This file exists only to test submitting GitHub **Reviews** via CLI (`gh pr review`) instead of posting plain PR comments.

If you see this in a PR, it should be closed without merge.

